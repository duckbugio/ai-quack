# Утка - Flappy Bird Clone

Веб-игра в стиле Flappy Bird, разработанная на React и TypeScript. Управляйте уткой, избегайте препятствий и набирайте очки!

## Описание

"Утка" - это классическая аркадная игра, где игрок управляет уткой, которая должна пролетать между препятствиями. Игра становится сложнее с каждым пройденным препятствием благодаря прогрессивной системе сложности.

### Основные возможности

- 🎮 Плавное управление уткой с физикой гравитации
- 🎯 Система подсчета очков с сохранением лучшего результата
- 🔊 Звуковые эффекты (прыжок, столкновение, набор очков)
- 📱 Адаптивный дизайн для мобильных устройств
- 🎨 Красивая графика с анимациями и эффектами частиц
- ⚡ Оптимизированная производительность (60 FPS)
- 🧪 Полное покрытие тестами

## Технологии

- **React 18+** - UI библиотека для создания компонентов
- **TypeScript 5+** - Типизированный JavaScript для надежности кода
- **Vite** - Современный сборщик для быстрой разработки
- **Canvas API** - Рендеринг игровой графики и анимаций
- **CSS Modules** - Стилизация компонентов
- **Vitest** - Фреймворк для тестирования
- **ESLint** - Линтинг кода
- **Prettier** - Форматирование кода

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd duck-game
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

## Запуск

### Режим разработки

Запустите dev-сервер:
```bash
npm run dev
```

Игра будет доступна по адресу `http://localhost:5173`

### Production сборка

Соберите проект для production:
```bash
npm run build
```

Предпросмотр production сборки:
```bash
npm run preview
```

### Другие команды

- `npm run lint` - Проверка кода линтером
- `npm run format` - Форматирование кода
- `npm test` - Запуск тестов в watch режиме
- `npm run test:ui` - Запуск тестов с UI интерфейсом
- `npm run test:run` - Запуск тестов один раз

## Управление

- **Пробел** или **Стрелка вверх** - прыжок утки
- **Клик мыши** или **Тап** - прыжок утки
- **Escape** - пауза (во время игры)

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Game/          # Игровые компоненты
│   │   └── GameCanvas.tsx
│   └── UI/            # UI компоненты (меню)
│       ├── MainMenu.tsx
│       ├── PauseMenu.tsx
│       ├── GameOverMenu.tsx
│       └── PerformanceOverlay.tsx
├── contexts/          # React контексты
│   └── GameContext.tsx
├── game/              # Игровая логика
│   ├── entities/      # Игровые сущности
│   │   ├── Duck.ts
│   │   └── Obstacle.ts
│   ├── systems/       # Игровые системы
│   │   ├── CollisionSystem.ts
│   │   ├── ObstacleManager.ts
│   │   ├── ParticleSystem.ts
│   │   └── ScoreSystem.ts
│   └── utils/         # Утилиты
│       ├── constants.ts
│       ├── SoundManager.ts
│       ├── PerformanceMonitor.ts
│       └── BrowserCompatibility.ts
├── hooks/             # Кастомные React хуки
│   ├── useGameLoop.ts
│   └── useKeyboard.ts
├── types/             # TypeScript типы
│   └── game.types.ts
├── assets/            # Ресурсы (звуки)
│   └── sounds/
├── App.tsx            # Главный компонент
└── main.tsx           # Точка входа
```

## Архитектура

### Игровой цикл

Игра использует классический игровой цикл на основе `requestAnimationFrame`:
1. **Update** - обновление состояния игры (позиции, физика, коллизии)
2. **Render** - отрисовка всех объектов на canvas

### Системы игры

- **Duck** - главный персонаж с физикой гравитации и прыжков
- **ObstacleManager** - управление генерацией и движением препятствий
- **CollisionSystem** - детекция столкновений между объектами
- **ScoreSystem** - подсчет очков и прогрессивное увеличение сложности
- **ParticleSystem** - визуальные эффекты при столкновениях
- **SoundManager** - управление звуковыми эффектами

### Состояние игры

Управление состоянием осуществляется через React Context (`GameContext`):
- `MENU` - главное меню
- `PLAYING` - игра в процессе
- `PAUSED` - игра на паузе
- `GAME_OVER` - игра окончена

## Тестирование

Проект включает полное покрытие тестами для основных игровых систем:

- Тесты для класса `Duck` (движение, прыжки, границы)
- Тесты для класса `Obstacle` (генерация, движение)
- Тесты для `ObstacleManager` (управление препятствиями)
- Тесты для `CollisionSystem` (детекция столкновений)
- Тесты для `ScoreSystem` (подсчет очков)
- Тесты для `GameContext` (управление состоянием)

Запуск тестов:
```bash
npm test
```

Подробное руководство по тестированию см. в [TESTING_GUIDE.md](./TESTING_GUIDE.md)

## Производительность

Игра оптимизирована для стабильной работы на 60 FPS:

- Отрисовка только видимых объектов
- Кэширование границ объектов для коллизий
- Эффективное использование `requestAnimationFrame`
- Фильтрация препятствий за экраном
- Оптимизация памяти (удаление неиспользуемых объектов)

Для включения overlay производительности:
```javascript
localStorage.setItem('duck-game-show-performance', 'true')
```

## Особенности реализации

### Прогрессивная сложность

- Скорость препятствий увеличивается каждые 10 очков (на 5%)
- Расстояние между препятствиями уменьшается каждые 20 очков (на 2%)
- Максимальный множитель сложности: 2.5x

### Адаптивность

- Автоматическое масштабирование canvas для мобильных устройств
- Поддержка touch событий
- Адаптация под разные размеры экрана
- Обработка изменения ориентации экрана

### Звуки

- Звук прыжка при нажатии
- Звук столкновения при попадании в препятствие
- Звук набора очков при прохождении препятствия
- Возможность включения/выключения звуков в меню

## Разработка

### Требования

- Node.js 18+
- npm или yarn

### Настройка окружения

1. Установите зависимости (см. раздел "Установка")
2. Настройте ESLint и Prettier (конфигурация уже включена)
3. Запустите dev-сервер

### Стиль кода

- Используется ESLint для проверки кода
- Prettier для форматирования
- TypeScript strict mode
- CSS Modules для стилизации

### Коммиты

Рекомендуется использовать осмысленные сообщения коммитов, описывающие изменения.

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Разработано как учебный проект для изучения React, TypeScript и игровой разработки.

---

**Приятной игры! 🦆**
