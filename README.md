# Утка - Flappy Bird Clone

Веб-игра в стиле Flappy Bird, разработанная на React и TypeScript. Цель игры - управлять уткой, избегая препятствий и набирая очки.

## Описание

"Утка" - это современная веб-игра, вдохновленная классической Flappy Bird. Игра реализована с использованием Canvas API для плавной отрисовки графики и React для управления состоянием и UI компонентами.

### Основные возможности

- 🎮 Плавное управление уткой с физикой гравитации
- 🎯 Система подсчета очков с сохранением лучшего результата
- 🎨 Красивая графика с анимациями и эффектами частиц
- 🔊 Звуковые эффекты (прыжок, столкновение, набор очков)
- 📱 Адаптивный дизайн для мобильных устройств
- ⚡ Оптимизированная производительность (60 FPS)
- 🎯 Прогрессивное увеличение сложности
- ⏸️ Система паузы
- 📊 Мониторинг производительности (опционально)

## Технологии

- **React 18+** - UI библиотека для создания компонентов
- **TypeScript 5+** - Типизированный JavaScript для надежности кода
- **Vite** - Современный сборщик для быстрой разработки
- **Canvas API** - Рендеринг игровой графики и анимаций
- **CSS Modules** - Стилизация компонентов
- **Vitest** - Фреймворк для тестирования

## Установка

1. Клонировать репозиторий:
```bash
git clone <repository-url>
cd duck-game
```

2. Установить зависимости:
```bash
npm install
```

## Запуск

### Режим разработки

Запустить dev-сервер с hot-reload:
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Production сборка

Создать оптимизированную сборку:
```bash
npm run build
```

Собранные файлы будут в директории `dist/`

### Предпросмотр production сборки

Запустить локальный сервер для предпросмотра production сборки:
```bash
npm run preview
```

## Управление

- **Пробел** или **Стрелка вверх** - прыжок утки
- **Клик мыши** или **Тап** - прыжок утки
- **Escape** - пауза (во время игры)

## Тестирование

Запустить тесты:
```bash
npm test
```

Запустить тесты с UI:
```bash
npm run test:ui
```

Запустить тесты один раз:
```bash
npm run test:run
```

## Линтинг и форматирование

Проверить код линтером:
```bash
npm run lint
```

Отформатировать код:
```bash
npm run format
```

## Архитектура проекта

```
src/
├── components/          # React компоненты
│   ├── Game/          # Основной игровой компонент
│   └── UI/            # UI компоненты (меню, кнопки)
├── contexts/          # React контексты для состояния
├── game/              # Игровая логика
│   ├── entities/      # Игровые сущности (Утка, Препятствия)
│   ├── systems/      # Игровые системы (физика, коллизии, частицы)
│   └── utils/         # Утилиты (константы, звуки, производительность)
├── hooks/             # Кастомные React хуки
├── types/             # TypeScript типы и интерфейсы
└── assets/            # Ресурсы (звуки)
```

### Основные компоненты

- **GameCanvas** - основной компонент игры, управляет canvas и игровым циклом
- **GameContext** - контекст для управления состоянием игры (счет, состояние, лучший результат)
- **Duck** - класс игрового персонажа с физикой и анимацией
- **Obstacle** - класс препятствий
- **ObstacleManager** - система генерации и управления препятствиями
- **CollisionSystem** - система детекции коллизий
- **ScoreSystem** - система подсчета очков
- **ParticleSystem** - система эффектов частиц
- **SoundManager** - менеджер звуковых эффектов

## Особенности реализации

### Производительность

- Оптимизированная отрисовка (только видимые объекты)
- Кэширование границ объектов для коллизий
- Эффективное использование requestAnimationFrame
- Мониторинг производительности в реальном времени

### Игровой процесс

- Прогрессивное увеличение сложности (скорость и расстояние между препятствиями)
- Плавная физика с гравитацией
- Точная система коллизий
- Эффекты частиц при столкновении

### Адаптивность

- Автоматическое масштабирование canvas для разных размеров экрана
- Поддержка touch событий для мобильных устройств
- Адаптивные UI компоненты

## Дополнительные возможности

### Мониторинг производительности

Для включения overlay производительности в консоли браузера выполните:
```javascript
localStorage.setItem('duck-game-show-performance', 'true');
```

После перезагрузки страницы будет отображаться overlay с метриками FPS, времени кадра и использования памяти.

### Звуковые эффекты

Звуки можно включить/выключить в главном меню. Звуковые файлы должны быть размещены в `public/sounds/`:
- `jump.mp3` - звук прыжка
- `hit.mp3` - звук столкновения
- `score.mp3` - звук набора очков

Для генерации звуков можно использовать скрипт `src/assets/sounds/generate-sounds.js`.

## Разработка

### Структура кода

Проект следует принципам:
- Разделение логики и представления
- Компонентный подход
- Типизация через TypeScript
- Модульная архитектура

### Тестирование

Проект включает unit-тесты для основных игровых систем:
- Тесты физики утки
- Тесты системы коллизий
- Тесты генерации препятствий
- Тесты подсчета очков
- Тесты контекста игры

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Разработано как учебный проект для изучения React, TypeScript и Canvas API.
