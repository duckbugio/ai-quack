# Duck Game

Игра в стиле Flappy Bird, реализованная на React, TypeScript и Canvas API. Управляйте уткой, избегая препятствий и набирая очки.

## 🎮 Описание

Duck Game - это браузерная игра, где игрок управляет уткой, которая должна пролетать между препятствиями. Игра включает в себя:

- Физику с гравитацией и прыжками
- Систему препятствий с динамической сложностью
- Систему подсчета очков и рекордов
- Звуковые эффекты
- Анимации и визуальные эффекты
- Оптимизированную производительность

## 🚀 Быстрый старт

### Требования

- Node.js 18+ 
- npm или yarn

### Установка

```bash
# Установка зависимостей
npm install
```

### Запуск

```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Просмотр продакшен сборки
npm run preview
```

## 🎯 Управление

- **Пробел** или **Клик мышью** - прыжок утки
- **P** или **Escape** - пауза/возобновление игры

## 🧪 Тестирование

```bash
# Запуск тестов
npm test

# Запуск тестов с UI
npm run test:ui

# Запуск тестов один раз
npm run test:run
```

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Game/           # Игровые компоненты
│   │   └── GameCanvas.tsx
│   └── UI/             # UI компоненты
│       ├── MainMenu.tsx
│       ├── PauseMenu.tsx
│       ├── GameOverMenu.tsx
│       └── PerformanceOverlay.tsx
├── contexts/           # React контексты
│   └── GameContext.tsx # Контекст состояния игры
├── game/               # Игровая логика
│   ├── entities/       # Игровые сущности
│   │   ├── Duck.ts     # Класс утки
│   │   └── Obstacle.ts # Класс препятствий
│   ├── systems/        # Игровые системы
│   │   ├── CollisionSystem.ts    # Система коллизий
│   │   ├── ObstacleManager.ts    # Менеджер препятствий
│   │   ├── ScoreSystem.ts        # Система подсчета очков
│   │   └── ParticleSystem.ts     # Система частиц
│   └── utils/          # Утилиты
│       ├── constants.ts          # Игровые константы
│       ├── SoundManager.ts       # Менеджер звуков
│       └── PerformanceMonitor.ts # Мониторинг производительности
├── hooks/              # React хуки
│   ├── useGameLoop.ts  # Хук игрового цикла
│   └── useKeyboard.ts  # Хук обработки клавиатуры
└── types/              # TypeScript типы
    └── game.types.ts   # Типы игры
```

## 🛠️ Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - сборщик и dev-сервер
- **Vitest** - тестовый фреймворк
- **Canvas API** - отрисовка игровой графики

## 📝 Скрипты

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка проекта
- `npm run preview` - предпросмотр продакшен сборки
- `npm run lint` - проверка кода линтером
- `npm run format` - форматирование кода
- `npm test` - запуск тестов
- `npm run test:ui` - запуск тестов с UI
- `npm run test:run` - однократный запуск тестов

## ⚙️ Настройки

Игровые параметры можно изменить в файле `src/game/utils/constants.ts`:

- `GRAVITY` - сила гравитации
- `JUMP_FORCE` - сила прыжка
- `OBSTACLE_SPEED` - скорость препятствий
- `PIPE_GAP` - расстояние между частями препятствия
- `PIPE_SPACING` - расстояние между препятствиями

## 🎨 Особенности

- **Оптимизированная производительность**: кэширование границ объектов, фильтрация видимых элементов
- **Адаптивная сложность**: игра становится сложнее с увеличением счета
- **Система частиц**: визуальные эффекты при столкновениях
- **Звуковые эффекты**: звуки прыжка, столкновения и прохождения препятствий
- **Мониторинг производительности**: включите через `localStorage.setItem('duck-game-show-performance', 'true')`

## 📚 Дополнительная документация

- `OPTIMIZATION_REPORT.md` - отчет по оптимизации производительности
- `TESTING_GUIDE.md` - руководство по тестированию
- `TASK_*.md` - отчеты по выполненным задачам

## 📄 Лицензия

Проект создан в образовательных целях.
